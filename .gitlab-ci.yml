image: registry.secoder.net/tool/deployer

stages:
  - build
  - test
  - deploy

# ---------------- 阶段 1：构建镜像 ----------------
build:
  stage: build
  script:
    - export BUILD_IMAGE_NAME=$CI_REGISTRY_IMAGE
    - export BUILD_IMAGE_TAG=$CI_COMMIT_REF_SLUG
    - export BUILD_IMAGE_USERNAME=$CI_REGISTRY_USER
    - export BUILD_IMAGE_PASSWORD=$CI_REGISTRY_PASSWORD
    - deployer build

# ---------------- 阶段 2：自动化测试配置 ----------------
.test:
  # ⚠️ 修改点 1：把原本的 3.11 改成你们用的 3.12
  image: python:3.12
  stage: test
  before_script:
    - pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt coverage pytest pycodestyle pylint

unit-test:
  extends: .test
  script:
    # ⚠️ 修改点 2：去掉了特定的 app 目录限制，让它在整个项目里找测试用例
    - coverage run -m pytest --junit-xml=xunit-reports/xunit-result.xml
    - ret=$?
    - coverage xml -o coverage-reports/coverage.xml
    - coverage report
    - exit $ret
  after_script:
    - SUFFIX=$RANDOM
    - curl "http://api.secoder.net/static/sonar-scanner.tar.gz" -s -o "/tmp/sonar-$SUFFIX.tar.gz"
    - tar -xf "/tmp/sonar-$SUFFIX.tar.gz"  -C /opt
    - /opt/sonar-scanner/bin/sonar-scanner

style-test:
  extends: .test
  allow_failure: true
  script:
    # ⚠️ 修改点 3：扫描当前目录下的所有 Python 文件
    - pycodestyle . 
    - PYCODESTYLE_RET=$?
    - pylint . 
    - PYLINT_RET=$?
    - if [ $PYCODESTYLE_RET \> 0 ]; then exit $PYCODESTYLE_RET; fi;
    - exit $PYLINT_RET

# ---------------- 阶段 3：自动化部署到 Secoder 容器 ----------------
deploy:
  stage: deploy
  script:
    # 这里调用了 Secoder 特有的指令，用新镜像替换旧的 UR-IM 容器
    - deployer dyno replace $CI_PROJECT_NAME "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG" "$REGISTRY_USER" "$REGISTRY_PWD"
  only:
    # ⚠️ 修改点 4：你们之前用的 dev 分支，所以这里改成了 dev
    - dev