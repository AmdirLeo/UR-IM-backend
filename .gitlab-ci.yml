# 尝试直接使用这个镜像，不带前面的 registry 前缀（有些平台内部会自动映射）
image: registry.secoder.net/tool/deployer

stages:
  - diagnostic

check-env:
  stage: diagnostic
  script:
    - echo "Project Name is $CI_PROJECT_NAME"
    - ls -R  # 列出所有文件，确认 Dockerfile 在不在
  only:
    - feat/cicd